# Automatically generated by nengo-bones, do not edit this file directly

language: python
python: 3.6
notifications:
  email:
    on_success: change
    on_failure: change
  slack:
    if: branch = master
    on_pull_requests: false
    on_success: change
    on_failure: always
    rooms:
      - secure: "bWVmSjW3kLZEEmJxpEmcrxlOFMVK24pYbgTZ2pjra0KMmwA2OJ11BsjdDW0fDEVd4JJGkxV3vOghsufRTrKE10reLHSM0Jtum8PYvlYk7riE8yUish0VCwxa8gYZpsH41Q7IlJQB04y5XxwYMqSnDepOAmB8C5babwYJkzfF5Dw="
cache: pip

dist: xenial

env:
  global:
    - SCRIPT="test"
    - TEST_ARGS=""
    - BRANCH_NAME="${TRAVIS_PULL_REQUEST_BRANCH:-$TRAVIS_BRANCH}"
    - PIP_USE_FEATURE="2020-resolver"
    - NUMPY="numpy"

jobs:
  include:
  -
    env:
      SCRIPT="static"
  -
    env:
      SCRIPT="test-coverage-scipy"
      TEST_ARGS="--memory --plots"
  -
    env:
      SCRIPT="test-coverage"
      TEST_ARGS="--plots"
    python: 3.5
  -
    env:
      SCRIPT="docs"
    addons:
      apt:
        packages:
          - graphviz
          - pandoc
  -
    env:
      NUMPY="numpy==1.13"
    if: branch =~ ^release-candidate-*
    cache: False
  -
    env:
      NUMPY="numpy==1.14"
    if: branch =~ ^release-candidate-*
  -
    env:
      NUMPY="numpy==1.15"
    if: branch =~ ^release-candidate-*
  -
    env:
      NUMPY="numpy==1.16"
    if: branch =~ ^release-candidate-*
  -
    env:
      NUMPY="numpy==1.17"
    if: branch =~ ^release-candidate-*
  -
    dist: bionic
    python: 3.7
  -
    dist: bionic
    python: 3.8
  - stage: deploy
    if: branch =~ ^release-candidate-* OR tag =~ ^v[0-9]*
    env: SCRIPT="deploy"
    cache: false
    deploy:
      - provider: pypi
        server: https://test.pypi.org/legacy/
        user: __token__
        password: $PYPI_TEST_TOKEN
        distributions: "sdist bdist_wheel "
        on:
          all_branches: true
          tags: false
          condition: $TRAVIS_BRANCH =~ ^release-candidate-*
      - provider: pypi
        user: __token__
        password: $PYPI_TOKEN
        distributions: "sdist bdist_wheel "
        on:
          all_branches: true
          tags: true
          condition: $TRAVIS_TAG =~ ^v[0-9]*

before_install:
  # export travis_terminate for use in scripts, from here:
  # https://github.com/travis-ci/travis-build/blob/master/lib/travis/build/bash/travis_terminate.bash
  - export -f travis_terminate
    _travis_terminate_agent
    _travis_terminate_freebsd
    _travis_terminate_linux
    _travis_terminate_osx
    _travis_terminate_unix
    _travis_terminate_windows
  # upgrade pip
  - pip install pip --upgrade
  # install/run nengo-bones
  - pip install git+https://github.com/nengo/nengo-bones#egg=nengo-bones
  - bones-generate --output-dir .ci ci-scripts
  - if [[ "$TRAVIS_PYTHON_VERSION" < "3.6" ]]; then
        echo "Skipping bones-check because Python $TRAVIS_PYTHON_VERSION < 3.6";
    else
        bones-check --verbose;
    fi
  # display environment info
  - pip freeze

install:
  - .ci/$SCRIPT.sh install
  - pip freeze

before_script:
  - .ci/$SCRIPT.sh before_script

script:
  - .ci/$SCRIPT.sh script

before_cache:
  - .ci/$SCRIPT.sh before_cache

after_success:
  - .ci/$SCRIPT.sh after_success

after_failure:
  - .ci/$SCRIPT.sh after_failure

before_deploy:
  - .ci/$SCRIPT.sh before_deploy

after_deploy:
  - .ci/$SCRIPT.sh after_deploy

after_script:
  - .ci/$SCRIPT.sh after_script
